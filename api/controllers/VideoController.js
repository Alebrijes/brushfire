/**
 * VideoController
 *
 * @description :: Server-side logic for managing videos
 * @help        :: See http://sailsjs.org/#!/documentation/concepts/Controllers
 */

module.exports = {

/*	addVideo: function (req, res) {

		var videoFromClient = req.params.all();

		if (req.isSocket && req.method === 'POST') {
			Video.create(videoFromClient)
			.exec(function(err, videoFrom) {
				console.log(videoFrom);
				Video.publishCreate({id: videoFrom.id, title: videoFrom.title, src: videoFrom.src});
			});
		}
		else if (req.isSocket) {
			Video.watch(req.socket);
			console.log('Nuevo video subido ' + req.socket.id);
		}
	} */

};

